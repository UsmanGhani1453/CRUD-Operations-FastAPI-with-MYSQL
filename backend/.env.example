# Copy this file to .env and fill in real values.
# .env is already gitignored - never commit real secrets.

ENVIRONMENT=development

# --- Database ---
DATABASE_URL=mysql+pymysql://1LPuqTsb1QVifGt.root:Mt7oMoAKfFWHNRCQ@gateway01.ap-southeast-1.prod.aws.tidbcloud.com:4000/test?ssl_verify_cert=true&ssl_verify_identity=true

# --- Auth ---
# Generate one with: python -c "import secrets; print(secrets.token_hex(32))"
SECRET_KEY=890c1fdcaddeef22ad0f776313038f022c1421992c67d3aeed49abcf50f70222
JWT_ALGORITHM=HS256
ACCESS_TOKEN_EXPIRE_MINUTES=120
EMAIL_VERIFICATION_TOKEN_EXPIRE_HOURS=24

# --- Email (SMTP) ---
# For Gmail: use an App Password, not your account password.
# https://myaccount.google.com/apppasswords
SMTP_HOST=smtp.gmail.com
SMTP_PORT=465
SENDER_EMAIL=usmanghanivhr1453@gmailcom
SENDER_PASSWORD=your-app-password
APP_BASE_URL=http://127.0.0.1:8000

# --- CORS ---
CORS_ALLOWED_ORIGINS=http://127.0.0.1:5500,http://localhost:5500

# --- Currency ---
# --- Currency ---
CURRENCY=PKR

# --- Frontend base URL ---
FRONTEND_BASE_URL=http://127.0.0.1:5500

# --- Seed admin (used by seed.py only) ---
SEED_ADMIN_EMAIL=admin@haak.com
SEED_ADMIN_PASSWORD=change-me
